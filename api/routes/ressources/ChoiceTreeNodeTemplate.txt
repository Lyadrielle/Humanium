{
  "choiceTree": {
    "":{
      "id": "kitchen-fight",              // ici tu mets ce que tu veux tant que l'id est unique
      "name": "Bataille pour le nutella", // cette info sera utile pour la timeline ou tout autre endroit où tu devras nomer la scène
      "video": "nutella_battle",          // le nom de la vidéo, elles seront toutes enregistrées en back dans le même dossier avec la même extension
      "type": "QTE",                      // il faut choisir entre QTE, choice et cinematic
      
      // à partir de là c'est les données qui varient en fonction du type choisi
      // pour cinematic
      "next": "nutella-toast",            // l'id de la scène suivante
      // pour QTE
      "success": "nutella-toast",         // l'id de la scène suivante si le QTE réussit
      "failure": "depression",            // l'id de la scène suivante si le QTE échoue
      // pour choice
      "text": "Que faire ?",              // la question qui s'affiche à l'écran ou le texte qui invite l'utilisateur à faire un choix
      "choices": [                        // un tableau de tous les choix possibles
        {
          "text": "Voler le pot de nutella",
          "next": "nutella-toast"
        },                                // un choix contient le texte associé et l'id de la scène sur laquelle il mène
        {
          "text": "Attendre qu'on m'en propose",
          "next": "depression"
        }
      ],
      "timer": "10",                      // le temps en secondes du timer, dans le cas présent le choix se déclenchera 10 secondes avant la fin de la vidéo
      "defaultChoice": 2                  // le choix par défault si l'utilisateur ne se décide pas, ici c'est le 3eme ne pas oublier que les tableaux commencent toujours à 0
    }
  }
}